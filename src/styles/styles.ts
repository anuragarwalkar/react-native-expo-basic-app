import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({});

export const globalStyles = StyleSheet.create({
  absoluteCenter: { flex: 1, justifyContent: 'center', alignItems: 'center' },
});
